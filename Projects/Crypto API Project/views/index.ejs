<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto API</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Crypto Prices</h1>
    <% if (cryptoData) { %>
        <ul>
            <% cryptoData.forEach(crypto => { %>
                <li><%= crypto.symbol %>: <%= crypto.last_trade_price %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>Could not fetch data. Try again later.</p>
    <% } %>
</body>
</html>
